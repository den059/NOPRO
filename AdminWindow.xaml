<Window x:Class="NOPRO.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Админ-панель: Редактор промптов" Height="600" Width="900"
        Background="{StaticResource MainBgColor}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Редактор системных промптов" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextColor}" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PromptsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextColor}"/>
                            <TextBox Text="{Binding Template, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" TextWrapping="Wrap"
                                     Height="80" Margin="0,5,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Загрузить из файла..." Click="LoadFromFile_Click" Margin="5"/>
            <Button Content="Сохранить в файл..." Click="SaveToFile_Click" Margin="5"/>
            <Button Content="Применить изменения" Click="ApplyChanges_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>